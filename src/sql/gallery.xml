<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gallery">
	<resultMap type="gal" id="galleryList">
		<result property="galleryNo" column="gallery_no" jdbcType="NUMERIC"/>
		<result property="nickname" column="nickname" jdbcType="VARCHAR"/>
		<result property="title" column="title" jdbcType="VARCHAR"/>
		<result property="writedate" column="writedate" jdbcType="VARCHAR"/>
		<result property="mergepath" column="mergepath" jdbcType="VARCHAR"/>
	</resultMap>
	
	<!-- 갤러리를 올리는 쿼리문  -->
	<insert id="insertGallery" parameterType="gal">
		<selectKey keyProperty="galleryNo" resultType="int" order="BEFORE">
			SELECT gallery_seq.nextVal FROM dual
		</selectKey>
		insert into gallery (gallery_no, nickname, title, writedate, mergepath)
		values (#{galleryNo}, #{nickname}, #{title}, #{writedate}, #{mergepath})
	</insert>
	
	<!-- 모든 갤러리를 띄우는 쿼리문  -->
	<select id="getAllGalleryList" parameterType="hashMap" resultMap="galleryList">
			SELECT gallery_no, nickname, title, writedate, mergepath, rank FROM
				(SELECT 
					rownum AS rank,
					gallery_no, 
					nickname, 
					title, 
					writedate, 
					mergepath
				FROM 
					(SELECT * FROM gallery ORDER BY gallery_no DESC)
				WHERE
					rownum &lt;= #{endArticle})
			WHERE
				rank &gt;= #{startArticle}
			ORDER BY
				rank ASC
	</select>
	
	<!-- 특정 갤러리 정보를 가져오는 쿼리문 -->
	<select id="getGalleryByGalleryNo" parameterType="_int" resultMap="galleryList">
		SELECT gallery_no, nickname, title, writedate, mergepath
		FROM gallery
		WHERE gallery_no = #{value}
	</select>
	
	<!-- 갤러리를 삭제하는 쿼리문  -->
	<delete id="deleteGallery" parameterType="_int">
		delete from gallery
		where gallery_no =#{value} 
	</delete>
	
	<!-- 모든 게시물 수를 반환하는 쿼리문 -->
	<select id="getSize" resultType="_int">
		SELECT count(*) FROM gallery
	</select>
</mapper>